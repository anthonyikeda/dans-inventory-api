spring:
  datasource:
    username: sa
    url: jdbc:hsqldb:file:inventory-database;shutdown=true
  flyway:
    locations: classpath:/db/specific/hsqldb

logging:
  level:
    com.cloudyeng.service.inventoryapi: debug
